class BackgroundWidgetHandler {
  const BackgroundWidgetHandler();

  Future<void> handleAction(String action) async {
    // Hook reserved for play, pause and next actions from native widgets.
  }
}
